// Example variables
let title = "Chart Title";
let hasAxes = true;

// Checking the condition
if (title || hasAxes) {
    console.log("Either title is present or hasAxes is true.");
} else {
    console.log("Neither title is present nor hasAxes is true.");
}
