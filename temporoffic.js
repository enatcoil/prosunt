// Define the range
let start = 3;
let end = 7;

// Calculate the sum using a loop
let total_sum = 0;
for (let num = start; num <= end; num++) {
    total_sum += num;
}

// Output the result
console.log(total_sum);
